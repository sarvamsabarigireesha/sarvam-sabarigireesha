# Sarvam Sabarigireesha — Clean Rebuild

Upload these files to the repository root. Keep the existing `assets` folder and its `logo.jpg`.

Hero image will be added only after this clean baseline is confirmed working.
